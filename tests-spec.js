'use strict';
var expect = require('chai').expect;
var LinkedLists = require('./linked-lists.js');

describe('LinkedLists', function() {
  it('should exist', function() {
    expect(LinkedLists).to.not.be.undefined;
  });
});
